#include <stdio.h>
#include<math.h>

int main()
{
    int a = 2, r = 3, n = 5, i, an;
    //printf("Enter the Starting Number: ");
    //scanf("%d",&a);
   // printf("Enter the Difference: ");
   // scanf("%d",&d);
  //  printf("Enter the range: ");
   // scanf("%d",n);

    for(i = 0; i <=n; i++)
    {
        printf("%d ", an);
        an= a*pow(r,i);
    }
    return 0;
}

