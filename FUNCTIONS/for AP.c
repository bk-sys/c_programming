#if 0
#include <stdio.h>

int main()
{
     int a=2, d=3, n=5, i,an;
    //printf("Enter the Starting Number: ");
    //scanf("%d",&a);
   // printf("Enter the Difference: ");
   // scanf("%d",&d);
  //  printf("Enter the range: ");
   // scanf("%d",n);

    for(i = 0; i < n; i++)
    {
        an= a + i * d;
        printf("%d ", an);
    }
    return 0;
}
#endif 
