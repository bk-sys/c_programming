#include <stdio.h>
typedef long long ll;  //typedef should be defined as global variable

int main()
{
    //using the typedef alias name to declare varialbe
    ll a=20;
    printf("%ld  ",a);
    return 0;
}
